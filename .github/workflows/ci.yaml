name: Spam Detector CI

# Trigger: Run this whenever I push code
on: [push]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    # 1. Download the code
    - name: Checkout code
      uses: actions/checkout@v3

    # 2. Install Python
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    # 3. Install libraries (FastAPI, Scikit-learn, etc.)
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install httpx  # Needed for TestClient

    # 4. Run the Model Training (Just to prove it works on cloud)
    - name: Retrain Model
      run: python src/train.py

    # 5. Run the Tests
    - name: Run Tests
      run: pytest tests/test_app.pysteps:
    # 1. Download the code
    - name: Checkout code
      uses: actions/checkout@v3

    # 2. Install Python
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    # 3. Install libraries
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install httpx

    # --- NEW STEP HERE ---
    # 4. Run Preprocessing (Create train.csv from spam.csv)
    - name: Preprocess Data
      run: python src/preprocess.py
    # ---------------------

    # 5. Run the Model Training
    - name: Retrain Model
      run: python src/train.py

    # 6. Run the Tests
    - name: Run Tests
      run: pytest tests/test_app.py